CREATE DATABASE IF NOT EXISTS پروژه;

USE پروژه;

CREATE TABLE IF NOT EXISTS جدول1 (
    ستون1 INT PRIMARY KEY,
    ستون2 VARCHAR(100),
    ستون3 DATE
);

CREATE TABLE IF NOT EXISTS جدول2 (
    ستون1 INT PRIMARY KEY,
    ستون2 VARCHAR(100),
    ستون3 DATE
);
